# Lefthook configuration

# pre-commit hooks 暂时禁用
# pre-commit:
#   parallel: true
#   commands:
#     lint:
#       glob: "*.js"
#       run: pnpm lint:fix
#     test:
#       run: pnpm test -- --passWithNoTests

# pre-push hooks 暂时禁用
# pre-push:
#   commands:
#     test:
#       run: pnpm test

commit-msg:
  commands:
    commitlint:
      run: |
        if command -v commitlint >/dev/null 2>&1; then
          commitlint --edit $1
        else
          echo "commitlint not found, skipping commit message validation"
        fi